'use strict';

module.exports = function(prompt){
    var prompt = "yes or no?";
    if (prompt = "yes"){
        alert("your answer was no.");
    } 
    else if (prompt = "no"){
        alert("your answer was yes.");
    }
    else {
        alert("try harder.");
    }
};